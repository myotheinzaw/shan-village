Continue and finish. Priority order:

1. Write the three missing admin pages — Users & roles, Settings (modules, masters, tolerances), Audit log — so the sidebar has no broken links and the build compiles cleanly.
2. Run a full typecheck and build, and fix every error. The app must build with zero errors before you stop.
3. Approved leave should write through to the roster.
4. Check the roster builder and staff view on a phone-width screen.

Then STOP and confirm the build is clean. Do not add more sample data — the real Shan Village roster data is coming in a later message and will replace the samples entirely.
